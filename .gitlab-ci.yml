stages:
  - staging

staging:
  stage: staging
  script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=i-say-stag --api-key=${HEROKU_API_KEY}
  only:
    - stagging-area
  tags:
    - docker
